// let numList: Array<Number>;

// numList = [1, 2, 20 , 6 , 10];

// let map = numList.filter( (num) => num <= 5);

// console.log(map);

function swapNumbers(num1: number , num2:number ){
    return num1 + num2;
}
